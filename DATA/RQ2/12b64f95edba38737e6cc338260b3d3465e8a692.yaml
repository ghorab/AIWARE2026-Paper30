apiVersion: v1
kind: Service
metadata:
  name: mock-ai
  annotations:
    beta.cloud.google.com/backend-config: '{"ports": {"http":"mock-ai-ingress"}}'
  labels:
    app: mock-ai
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http-server
  selector:
    app: mock-ai
