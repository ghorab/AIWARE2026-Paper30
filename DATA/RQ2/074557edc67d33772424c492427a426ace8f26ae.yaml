apiVersion: v1
kind: Service
metadata:
  namespace: benchmarks
  name: nestapploadbalancer
spec:
  selector:
    app: nestapp
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 3000
    - name: https
      port: 443
      targetPort: 3000
